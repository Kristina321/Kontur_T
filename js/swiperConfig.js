/**
* Конфигурация для библиотеки Swiper
*/
export const swiperConfig = {
  loop: true,
  /*autoplay: {
    delay: 10000,
    disableOnInteraction: false,
  },*/
  centeredSlidesBounds: true,
  grabCursor: true,
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },
}
